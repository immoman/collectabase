# Collectabase – non-sensitive defaults for Docker deployment
# API keys (IGDB, eBay, RAWG, PriceCharting) are configured
# through the Settings page in the app UI.

DATABASE_URL=sqlite:////app/data/games.db
UPLOADS_DIR=/app/uploads
TZ=Europe/Berlin
